# MoveLite 설치 및 사용 가이드

본 문서는 MKS-ESP32-FOC V1.0 보드 기반 2륜 로봇 `MoveLite`의 펌웨어 설치부터 웹 인터페이스 사용 방법까지 안내합니다.

---

## 1. 하드웨어 연결 확인

전원을 인가하기 전에 배선이 다음 핀 맵에 맞게 연결되어 있는지 확인합니다.

| 부품 | 핀 연결 (MKS-ESP32-FOC V1.0 기준) |
|---|---|
| **Motor L (M0)** | 보드 내장 (UH=32, VH=33, WH=25, EN=22) |
| **Motor R (M1)** | 보드 내장 (UH=26, VH=27, WH=14, EN=12) |
| **AS5600 L (M0)** | I2C0: SDA=19, SCL=18 (보드 I2C 헤더) |
| **AS5600 R (M1)** | I2C1: SDA=23, SCL=5 (보드 I2C 헤더) |
| **MPU6050** | I2C0: SDA=19, SCL=18 (AS5600 L과 병렬 연결) |
| **VL53L0X 전방** | I2C0 (19/18), XSHUT=GPIO 4 |
| **VL53L0X 후방** | I2C0 (19/18), XSHUT=GPIO 2 |

> ⚠️ **주의:** ToF 센서의 XSHUT 핀이 제대로 연결되지 않으면 I2C 주소(0x29) 충돌로 인해 시스템이 멈춥니다.

---

## 2. 펌웨어 및 웹 UI 설치

`MoveLite`는 실행 코드(펌웨어)와 웹 파일 시스템(LittleFS) 두 가지를 모두 업로드해야 합니다.

### 2.1 WiFi 설정 변경
`src/main.cpp` 파일을 열어 본인의 WiFi 환경에 맞게 공유기 이름과 비밀번호를 수정합니다.
```cpp
const char* WIFI_SSID = "YOUR_SSID";
const char* WIFI_PASS = "YOUR_PASS";
```

### 2.2 펌웨어 업로드 (Upload)
1. 로봇을 PC에 USB로 연결합니다.
2. PlatformIO 좌측 메뉴(개미 아이콘)에서 **Project Tasks -> movelite -> General -> Upload**를 클릭합니다.
3. 업로드가 완료되면 `SUCCESS` 메시지를 확인합니다.

### 2.3 파일 시스템 업로드 (Upload Filesystem Image)
1. 웹 UI 파일(`index.html`, `app.js`)은 `data/` 폴더에 저장되어 있습니다.
2. PlatformIO 좌측 메뉴에서 **Project Tasks -> movelite -> Platform -> Build Filesystem Image**를 실행합니다.
3. 이어서 **Upload Filesystem Image**를 클릭하여 ESP32의 내장 플래시(LittleFS) 영역에 웹 파일을 전송합니다.

---

## 3. 초기 기동 및 접속

1. 펌웨어와 파일 시스템이 모두 업로드되면, VSCode 하단의 **Serial Monitor** 아이콘(플러그 모양 옆)을 클릭하여 시리얼 모니터를 엽니다. 통신 속도는 `115200`입니다.
2. ESP32가 부팅하면서 다음과 같은 과정을 수행합니다:
   - I2C 센서 초기화 (IMU, ToF)
   - 모터 정렬 (삐- 소리와 함께 모터가 살짝 움직입니다)
   - WiFi 접속
3. WiFi 접속이 완료되면 시리얼 모니터에 IP 주소가 출력됩니다.
   ```
   WiFi connected. IP: 
   192.168.1.XXX
   ```
4. PC 또는 스마트폰의 웹 브라우저를 열고 `http://192.168.1.XXX` 로 접속합니다.

---

## 4. MoveLite Dashboard 사용 방법

웹 브라우저에 접속하면 검은색 배경의 맵 캔버스와 조이스틱이 보입니다.

### 4.1 상태 확인
- **Telemetry 패널**: 우측 하단에서 X, Y (위치), Theta (로봇 방향), 전후방 ToF 측정 거리(m)가 실시간으로 변하는지 확인합니다.
- 로봇을 손으로 살짝 움직였을 때 X, Y, Theta 값이 변한다면 오도메트리(AS5600 + MPU6050)가 정상 동작하는 것입니다.

### 4.2 수동 조종 (조이스틱)
- Mode가 `IDLE` 인지 확인합니다.
- 화면의 가상 조이스틱을 마우스로 드래그(스마트폰은 터치)하여 로봇이 정상적으로 앞/뒤/좌/우로 움직이는지 테스트합니다.
- ⚠️ 만약 로봇이 반대 방향으로 가거나 제자리에서 돈다면, `main.cpp`의 160라인 `motor_r.setVelocity(-r_rads);` 의 부호(`-`)를 로봇 물리적 모터 장착 방향에 맞춰 수정해야 합니다.

### 4.3 맵핑 시작 (Start Mapping)
1. **[Start Mapping]** 버튼을 클릭합니다. Mode가 `MAPPING`으로 변경됩니다.
2. 조이스틱을 이용하여 로봇을 방 안에서 이리저리 이동시킵니다.
3. 전후방 ToF 센서가 벽이나 장애물(빨간색 점)을 감지하고 빈 공간(흰색)을 캔버스 위에 그려나갑니다.
4. 로봇은 방향을 바꾸어가며(제자리 회전) 스캔해야 좌우 빈 공간을 맵핑할 수 있습니다.

### 4.4 자율 주행 (Navigation)
1. 맵이 어느 정도 완성되면 **[Stop Mapping]** 버튼을 누릅니다. Mode가 `MAP_READY`로 변경됩니다.
2. 맵 캔버스 위의 빈 공간(흰색 또는 검은색 영역 중 장애물이 없는 곳)을 클릭합니다.
3. Mode가 `NAVIGATING`으로 바뀌면서 로봇이 클릭한 목표 지점(Goal)을 향해 스스로 경로를 계획(A*)하고 이동(Pure Pursuit)합니다.
4. 도착 시 다시 `MAP_READY` 모드로 돌아옵니다.
5. 초기화가 필요하면 **[Reset]** 버튼(구현 시 재부팅 또는 변수 초기화 연동)을 누릅니다.

---

## 5. 트러블슈팅

- **"Motor L Init Failed!" 에러가 뜹니다.**
  - AS5600 엔코더 자석과 센서 간격이 적절한지(약 1~2mm), 12V 전원이 제대로 인가되었는지 확인하세요.
- **맵에서 로봇이 비스듬히 갈 때 벽이 여러 겹으로 그려집니다.**
  - 바퀴가 미끄러지면서 오도메트리 오차가 누적되는 현상입니다. 바닥 마찰력을 높이거나 가속도를 줄여서 주행하세요.
- **웹 페이지가 열리지 않아요.**
  - 시리얼 모니터에서 WiFi 접속 여부 및 IP를 다시 확인하고, PC와 ESP32가 동일한 와이파이(SSID) 망에 접속되어 있는지 확인하세요.
- **Upload Filesystem Image가 실패합니다.**
  - 시리얼 모니터 창이 열려있으면 포트 점유로 인해 실패할 수 있습니다. 휴지통 아이콘을 눌러 시리얼 터미널을 닫고 다시 시도하세요.

---

## 6. 개별 컴포넌트 하드웨어 테스트

전체 시스템을 동작시키기 전에 개별 모듈(모터, 센서)이 정상적으로 동작하는지 시리얼 부팅 메뉴를 통해 쉽게 테스트할 수 있습니다. 코드를 수정할 필요 없이 기본 펌웨어에 내장되어 있습니다.

**테스트 방법:**
1. PlatformIO에서 **Upload**를 통해 펌웨어를 업로드합니다 (이미 되어 있다면 로봇의 EN 버튼을 눌러 재부팅합니다).
2. VSCode 하단의 **Serial Monitor** (115200 bps)를 엽니다.
3. 부팅 직후 **5초 동안** 아래와 같은 테스트 메뉴가 출력됩니다.

```text
========================================
  MoveLite Hardware Test Menu
========================================
 [1] Test Encoders (AS5600)
 [2] Test Motor PWM (Open Loop)
 [3] Test Motor Velocity (Closed Loop)
 [4] Test IMU (MPU6050)
 [5] Test ToF (VL53L0X)
----------------------------------------
Press 1-5 within 5 seconds to run a test...
```

4. 5초 안에 시리얼 입력창에 원하는 번호(1~5)를 입력하고 엔터를 누르면 해당 테스트가 무한 반복 실행됩니다.
5. 다른 테스트를 하려면 로봇을 리셋(EN 버튼)하고 다른 번호를 누르세요.
6. 아무것도 누르지 않고 5초가 지나면 정상적인 MoveLite 메인 시스템으로 자동 부팅됩니다.

| 번호 | 설명 |
|---|---|
| `1` | AS5600 엔코더 I2C 통신 및 각도 읽기 테스트. 손으로 바퀴를 돌렸을 때 숫자가 변해야 합니다. |
| `2` | 모터 Open-Loop (전압 기반) 구동 테스트. 엔코더 없이 모터에 신호만 주어 물리적으로 회전하는지 확인합니다. |
| `3` | 모터 Closed-Loop 속도 제어 테스트. 시리얼 입력창에 `T 5` 를 입력하면 5 rad/s 로 회전합니다. |
| `4` | MPU6050 상보 필터가 적용된 Z축(Yaw) 각도 측정 테스트. 보드를 수평으로 회전시키면 각도가 변해야 합니다. |
| `5` | 전/후방 VL53L0X 거리 센서 테스트. 센서 앞에 손을 대면 거리가 변하는지 확인합니다. (XSHUT 핀 동작 확인용) |

---

## 7. 현재 구현의 한계점 및 예상 성능

본 펌웨어는 "가벼운 맵핑과 자율주행(SLAM-lite)"을 컨셉으로 제작되었으며, 하드웨어 사양(ESP32-WROOM) 및 경량화 설계로 인한 몇 가지 한계가 존재합니다.

### 7.1 구조 및 알고리즘적 한계
1. **제한된 맵 사이즈 (SRAM 한계)**
   - WROOM 모듈은 PSRAM이 없기 때문에 메모리가 매우 제한적입니다. 
   - 현재 맵 해상도는 **10cm(0.1m) 당 1셀**, 맵 크기는 **100x100 셀 (10m x 10m)**로 하드코딩되어 있습니다.
   - 이 경우 Occupancy Grid 맵 1개당 10KB를 소모하며, A* 연산을 위한 인플레이션 맵까지 고려하면 이 수치가 현재 ESP32가 안정적으로 돌릴 수 있는 마지노선입니다.
2. **센서의 사각지대 (측면 맹점)**
   - 고가의 360도 2D Lidar 대신 1D ToF(VL53L0X) 2개만 앞뒤로 부착되어 있습니다.
   - 따라서 로봇은 전진/후진 시의 장애물만 감지할 수 있으며 측면 장애물은 볼 수 없습니다. 맵핑 시 제자리에서 회전(스핀)하며 스캔하는 과정이 필수적입니다.
   - ToF 센서의 감지 거리는 최대 약 **1.2m** 로 짧은 편입니다.
3. **누적되는 오도메트리 드리프트**
   - 현재 구현은 IMU(가속도+자이로 상보필터)와 바퀴 엔코더만을 사용하여 위치를 추정합니다.
   - 고가의 SLAM처럼 '스캔 매칭(Scan Matching)'이나 '루프 클로저(Loop Closure)' 알고리즘을 통한 맵 보정이 없기 때문에, 오래 주행할수록 로봇의 실제 위치와 맵 상의 위치가 미세하게 틀어지는 오차(Drift)가 누적됩니다. 

### 7.2 예상 처리 속도 (FPS) 및 CPU 부하
ESP32의 듀얼 코어(Core 0, Core 1) 분리 설계를 통해 각 작업이 독립적으로 실행되도록 최적화되었습니다.

- **Core 1 (하드 리얼타임 제어 태스크)**
  - `loopFOC()` (모터 전류/속도 제어): 최우선 순위로 딜레이 없이 무한 루프로 실행되며, **약 2kHz~5kHz (0.2~0.5ms)**의 속도로 매우 부드럽게 모터를 제어합니다.
  - `Odometry & IMU 업데이트`: **100Hz (10ms)** 간격으로 실행됩니다.
  - `ToF 스캔 및 경로 추종(Pure Pursuit)`: **20Hz (50ms)** 간격으로 실행됩니다.
  - *병목 주의:* A* 경로 탐색은 사용자가 목표 지점을 클릭할 때만 실행되며, 100x100 맵 기준 최대 탐색 시 **약 10~30ms** 정도 소요될 수 있습니다. 이때 순간적으로 FOC 제어 루프가 지연될 수 있습니다.
  - **예상 CPU 사용량:** 평상시 약 40~50%

- **Core 0 (통신 및 웹 서버 태스크)**
  - 로봇의 현재 상태(위치, 방향, 모드)와 맵 데이터(Base64 인코딩)를 WebSocket을 통해 브라우저로 전송합니다.
  - 전송 주기는 **20Hz (20 FPS)**로 고정되어 있어 부드러운 웹 UI 애니메이션을 제공합니다.
  - WiFi 스택과 비동기 웹서버가 백그라운드에서 동작합니다.
  - **예상 CPU 사용량:** 유휴 시 10%, 맵 데이터 전송(스트리밍) 시 최대 40~50%
